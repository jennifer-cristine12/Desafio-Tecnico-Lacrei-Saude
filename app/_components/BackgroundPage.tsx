import { Inicio } from "./Style";
export default function BackgroundImage({ children }: { children: React.ReactNode }) {
    return (
        <Inicio>
            {children}
        </Inicio >
    );
}